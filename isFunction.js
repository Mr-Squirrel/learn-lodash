/**
 * @param {value} value - the value to check if it is a function
 * @returns {boolean} - returns true if value is a function
 */

export default function isFunction(value) {
  return typeof value === typeof Function

}
