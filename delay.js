/**
 * @param {function} func - the function to execute
 * @oaram {number} wait - the time to wait
 * @oaram [args] args
 * @returns {number} timer - the time to wait before executing function
 */

export default function delay(function(){}, wait, [args]) {
  return
}
